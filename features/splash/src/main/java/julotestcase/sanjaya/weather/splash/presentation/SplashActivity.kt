package julotestcase.sanjaya.weather.splash.presentation

import android.annotation.SuppressLint
import android.os.Bundle
import androidx.activity.ComponentActivity

/**
 * An activity that is displayed when the app is launched and is responsible for showing a splash screen.
 *
 * @constructor Creates a new instance of [SplashActivity].
 */
@SuppressLint("CustomSplashScreen")
class SplashActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
    }
}
